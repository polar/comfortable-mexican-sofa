<h1><%= t('.title') %></h1>

<%= comfy_form_for @file, :as => :file, :url => cms_admin_site_file_path(@file, :site_id => @site.id) do |form| %>
  <%= render :partial => 'form', :object => form %>
<% end %>